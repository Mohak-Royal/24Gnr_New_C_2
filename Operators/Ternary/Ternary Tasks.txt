Write a C program that uses the ternary operator to find the maximum of two numbers provided by the user.
Write a C program that uses the ternary operator to check if a number is even or odd.
Write a C program that uses the ternary operator to find the absolute value of a number.
Write a C program that uses the ternary operator to determine if a person is eligible to vote based on their age.
Write a C program that uses the ternary operator to determine if a number is positive, negative, or zero.
Write a C program that uses the ternary operator to find the smallest of three numbers provided by the user.

Write a C program that uses the ternary operator to determine the grade (A, B, C, D, or F) based on the marks entered by the user.

Write a C program that uses the ternary operator to check if a character entered by the user is a vowel or a consonant.


Write a C program that calculates the price of an item after a discount. If the price is above $100, apply a 10% discount, otherwise, apply a 5% discount.
Write a C program that calculates the Body Mass Index (BMI) and determines the category (Underweight, Normal weight, Overweight, or Obese) using the ternary operator.